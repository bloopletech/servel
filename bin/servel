#!/usr/bin/env ruby

require "servel"

mapping = {}

ARGV.each do |arg|
  root, url_root = arg.split(":" , 2)
  root = Pathname.new(root).realpath
  mapping[root] = url_root
end

servel = Servel::Servel.new(mapping)
servel.start